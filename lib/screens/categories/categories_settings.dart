import 'package:flutter/material.dart';
import 'package:ordering_app/screens/categories/views/categories_settings_list.dart';

class CategorySettings extends StatelessWidget {
  const CategorySettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CategorySettingsList();
  }
}
